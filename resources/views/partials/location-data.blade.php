[locationData('{{ $_listing->url() }}',[@foreach($_listing->categories()->orderBy('parent_id','asc')->get() as $category)'{{ $category->title }}',@endforeach],'{{$_listing->image->medium_url}}','{{$_listing->title}}', "{{number_format($_listing->price,2,',','.')}} ₺ @if($_listing->price_type == 'monthly') / Ay @elseif($_listing->price_type == 'yearly') / Yıl @endif" ,"{{$_listing->state->title}}, {{$_listing->city->title}}, {{$_listing->district->title}}"),{{$_listing->latitude}} ,{{$_listing->longitude}}, 0, markerIcon],